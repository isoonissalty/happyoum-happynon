# Oum & Non - Wedding Invitation

The site lives entirely in `site/` as static HTML/CSS with no build step. To deploy, enable GitHub Pages on this repo with the source set to the `site/` folder (Settings -> Pages -> Deploy from a branch -> folder `/site`), or copy the contents of `site/` to the repo root or a `docs/` folder if your Pages setup requires one of those instead.
